<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pms.admin.modules.admin.dao.SysRoleDao">

    <!-- 批量插入气象站数据 -->
	<select id="getRoleList" resultType="com.pms.common.pojo.SysRole">
	    SELECT
			a.id,
			a.role_code,
			a.role_name,
			a.enable,
			a.remark,
			a.sort,
			a.tenant_id
		FROM
			sys_role a,
			sys_user_role b
		WHERE
			a.id = b.role_id
		and 
		  b.user_id = #{id}
  	</select>
  	
</mapper>

